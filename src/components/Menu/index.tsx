import { MenuButton } from "./MenuButton";

export function Menu() {
  return (
    <nav className="w-full h-15 bg-red-400">
      <MenuButton />
      <MenuButton />
      <MenuButton />
      <MenuButton />
      <MenuButton />
      <MenuButton />
    </nav>
  );
}
