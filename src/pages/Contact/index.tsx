import { MainTemplate } from "../../templates/MainTemplate";

export function Contact() {
  return (
    <MainTemplate>
      <section className="p-30 bg-slate-100 text-slate-900">
        <h1 className="text-[40px] font-bold">Contatos</h1>
      </section>
    </MainTemplate>
  );
}
